import { isType } from '../../objects/objects.js';

export function receive(game, game_player, data)
{
	const objects = game.objects;

	for(const [s_id, object] of objects)
	{
		
	}
}

export const receiver = 'terrain-request';
